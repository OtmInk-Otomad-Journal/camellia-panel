export const address = "http://" + window.location.hostname;
export const port = "7215";

export const web_prefix_address = "http://localhost";
export const web_prefix_port = "7213";

export const panel_prefix = address + ":" + web_prefix_port;

export const web_prefix = `${web_prefix_address}:${web_prefix_port}`;
